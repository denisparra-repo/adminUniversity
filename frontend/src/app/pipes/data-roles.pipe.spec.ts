import { DataRolesPipe } from './data-roles.pipe';

describe('DataRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new DataRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
